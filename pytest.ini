[pytest]
testpaths = tests

# The repo root is importable for `autosetter` and `tests.fixtures`
pythonpath = .

# The pipeline's internal classes are named TestPipeline / TestReport / TestCase
# (referring to problem test cases, not unit tests). Without this, pytest attempts to collect them.
python_classes = _NeverCollectClasses
addopts = -q

# AutoSetter dependencies
#
# ollama       - official Ollama Python client (used for both vision and
#                text chat calls against the local Ollama server)
# Pillow       - image loading/validation/normalization for png/jpg/jpeg
# PyMuPDF      - offline PDF -> image rasterization (imported as `fitz`)

ollama>=0.4.0
Pillow>=10.0.0
PyMuPDF>=1.24.0

# HuggingFace Second Model (fine-tuned Qwen 7B for Codeforces artifacts)
#
# torch        - PyTorch tensor operations and GPU memory management
# transformers - HuggingFace Transformers for AutoModelForCausalLM + AutoTokenizer
# accelerate   - automatic device mapping (multi-GPU, mixed-precision)
torch>=2.0.0
transformers>=4.40.0
accelerate>=0.30.0

# Development / Testing
pytest>=8.0.0
# AutoSetter dependencies
#
# ollama       - official Ollama Python client used for both vision and

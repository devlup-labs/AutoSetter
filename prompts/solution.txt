You are a world-class competitive programmer (Grandmaster level) writing a
REFERENCE / MODEL solution for a judge's internal use.

Below is the structured JSON specification of the problem:

{JSON}

Your task: write a single, complete, correct, and OPTIMAL C++17 solution to
this problem.

REQUIREMENTS:
1. Analyze the "constraints" field carefully and choose an algorithm and
   data structures whose time and memory complexity comfortably fit within
   "time_limit" and "memory_limit" for the largest allowed input.
2. The solution must read input EXACTLY according to "input_format" and
   write output EXACTLY according to "output_format".
3. Use fast I/O appropriate for competitive programming
   (e.g. `ios_base::sync_with_stdio(false); cin.tie(nullptr);`, or scanf/printf).
4. Handle all edge cases implied by the constraints (minimum values,
   maximum values, empty/degenerate cases, overflow-prone arithmetic --
   use 64-bit integer types wherever values could exceed 32-bit range).
5. Prefer the asymptotically optimal known approach for this class of
   problem; briefly justify the chosen complexity in a short comment block
   at the top of the file.
6. The code must compile cleanly under `g++ -std=c++17 -O2` with no
   warnings from `-Wall -Wextra`.
7. Verify mentally against every sample in "samples" that your solution
   would produce the exact expected output for the exact given input
   before finalizing your answer.
8. Do not use non-standard, deprecated, or compiler-specific extensions
   beyond widely-supported GCC builtins (e.g. `__builtin_popcount` is fine).
9. Include the standard library headers you actually need (or a single
   `#include <bits/stdc++.h>` is acceptable for competitive programming).
10. Use `using namespace std;` if convenient; keep the code readable with
    meaningful variable names and concise comments on non-obvious logic.

OUTPUT RULES:
- Output ONLY the raw C++ source code for solution.cpp.
- Do NOT wrap the code in markdown code fences.
- Do NOT include any explanation, preamble, or commentary outside the code
  (comments belong inside the code as C++ `//` or `/* */` comments).
- The output must be a single, complete, compilable .cpp file with a
  `main()` function.

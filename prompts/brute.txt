You are an expert competitive programming problem-setter writing a
BRUTE-FORCE reference solution, used only as an independent correctness
oracle to stress-test the optimal solution -- never submitted to a judge
and never bound by the stated time/memory limits.

Below is the structured JSON specification of the problem:

{JSON}

Your task: write a single, complete C++17 program, `brute.cpp`, that solves
this problem using the most OBVIOUSLY CORRECT approach available --
straightforward simulation, brute-force enumeration, nested loops, or naive
recursion -- prioritizing correctness and simplicity over performance.

REQUIREMENTS:
1. The algorithm must be as simple and directly derived from the problem
   statement as possible, so its correctness is obvious on inspection.
   Prefer exhaustive enumeration / direct simulation over any cleverness.
   Slow, even exponential or O(n^3) complexity, is expected and acceptable.
2. Read input EXACTLY according to "input_format" and write output EXACTLY
   according to "output_format" -- byte-for-byte the same I/O contract as
   the optimal solution, so the two programs' outputs are directly
   comparable on the same input.
3. Do NOT apply the optimized algorithmic idea, data structure, or
   mathematical shortcut that an efficient solution to this class of
   problem would use. If you find yourself reaching for anything smarter
   than direct enumeration/simulation, stop and use the naive approach
   instead.
4. Do NOT worry about "time_limit" or "memory_limit" -- this program is
   only ever run on small/medium stress-test inputs, never on inputs sized
   at the full stated constraints.
5. Still handle every edge case correctly (minimum values, empty/degenerate
   cases, overflow-prone arithmetic -- use 64-bit integer types wherever
   values could exceed 32-bit range even in a naive computation).
6. Verify mentally against every sample in "samples" that this brute-force
   program would produce the exact expected output for the exact given
   input before finalizing your answer.
7. The code must compile cleanly under `g++ -std=c++17 -O2` with no
   warnings from `-Wall -Wextra`.
8. Include a short comment at the top explaining the brute-force approach
   and roughly what input size it remains practical for (e.g. "O(n^3),
   practical up to n ~ 200").
9. Include the standard library headers you actually need (or a single
   `#include <bits/stdc++.h>` is acceptable).

OUTPUT RULES:
- Output ONLY the raw C++ source code for brute.cpp.
- Do NOT wrap the code in markdown code fences.
- Do NOT include any explanation, preamble, or commentary outside the code
  (comments belong inside the code as C++ `//` or `/* */` comments).
- The output must be a single, complete, compilable .cpp file with a
  `main()` function.

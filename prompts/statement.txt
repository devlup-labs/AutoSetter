You are an expert competitive programming problem-setter, in the style used
by Codeforces / ICPC / IOI problem statements.

Below is a structured JSON specification of a competitive programming
problem, extracted from the original problem statement image:

{JSON}

Using ONLY the information in this JSON (do not invent new constraints,
samples, or story elements), write a clean, well-formatted, publication-
ready Markdown problem statement.

The Markdown document MUST follow this structure:

# <title>

**Time limit:** <time_limit>
**Memory limit:** <memory_limit>

## Problem Statement

<the "story" field, rewritten clearly and grammatically, preserving all
technical/mathematical details and meaning -- do not remove any
information, only improve clarity and flow>

## Input

<the "input_format" field, formatted clearly, using a bulleted or
numbered list if the original format has multiple distinct lines of input>

## Output

<the "output_format" field, formatted clearly>

## Constraints

<the "constraints" field, formatted as a bulleted list with each bound on
its own line, using Markdown math-friendly notation, e.g. `1 <= n <= 10^5`>

## Examples

For EACH entry in "samples", render:

### Example <index, starting at 1>

**Input:**
```
<sample input, verbatim>
```

**Output:**
```
<sample output, verbatim>
```

<If "explanation" is non-empty for this sample, add:>
**Explanation:** <explanation text>

## Notes

<the "notes" field, if non-empty; omit this whole section if "notes" is empty>

FORMATTING RULES:
1. Output ONLY the final Markdown document. Do not wrap it in code fences.
   Do not add any preamble like "Here is the statement".
2. Use proper Markdown syntax throughout (headings, code blocks, bold text).
3. Keep all sample inputs/outputs byte-for-byte identical to the JSON --
   never modify, reformat, or "fix" sample data.
4. If a field in the JSON is empty, omit the corresponding section rather
   than emitting an empty heading.
5. Write in clear, professional, contest-quality English.

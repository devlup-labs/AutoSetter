You are an expert competitive programming problem-setter specializing in writing robust, reproducible test-case generators using testlib.h (the standard library used by Codeforces Polygon and similar judge systems).

Below is the structured JSON specification of the problem:

{JSON}

Your task: write a complete, compilable `generator.cpp` that produces a single random (but always VALID per "constraints") test case for this problem, printed to stdout in exactly the format described by "input_format" and "samples".

REQUIREMENTS & BEHAVIOR:
1. Always include:
   #include "testlib.h"
   #include <iostream>
   #include <string>
   #include <vector>
   #include <algorithm>
   using namespace std;
2. Initialize `main` with `registerGen(argc, argv, 1);` as the first statement.
3. Use testlib's random engine ONLY:
   - For integers: `rnd.next(min_val, max_val)`
   - For 64-bit integers: `rnd.next(min_ll, max_ll)`
   - For floating-point: `rnd.next(min_d, max_d)`
   - For random strings: NEVER use testlib regex generation like `rnd.next("[a-z.*]{1,20}")` because it crashes on special characters. ALWAYS generate strings manually using a `for` loop and picking characters randomly with `rnd.next('a', 'z')` or `rnd.any(...)`.
   - For shuffling/permutations: `rnd.shuffle(vec.begin(), vec.end())`
   - STRICTLY FORBIDDEN: Do NOT use `rand()`, `srand()`, `<random>`, `std::mt19937`.
4. NO HARDCODED STATIC SAMPLES:
   - Always generate realistic random valid data matching the constraints. Never simply hardcode the problem statement's sample string or array.
5. Command-Line Arguments & Edge Cases:
   - Safely read parameters from `argv` with fallback defaults using `opt<T>("param", default_value)`.
   - Support `string type = opt<string>("type", "rand");` to produce stress edge cases:
     - `type == "min"`: produce minimum allowed bounds.
     - `type == "max"`: produce maximum allowed bounds.
     - `type == "rand"` (or default): produce uniform random valid test cases.
6. Exact Formatting & Whitespace (CRITICAL):
   - Strictly match the line and token layout specified in "input_format" and "samples".
   - TEST CASE COUNT: If the problem has `t` test cases, you MUST explicitly output `t` followed by a newline before printing the actual test cases.
   - NO TRAILING SPACES: Never print a space at the end of a line. When printing elements in a loop (like arrays or matrices), you MUST use conditional logic (e.g., `if (i < n - 1) cout << " ";`) to ensure the very last element is immediately followed by a newline, NOT a space.
   - Terminate the final output with a newline `\n`.
7. The code must compile cleanly under `g++ -std=c++17 -O2`.

OUTPUT RULES:
- Output ONLY the complete raw C++ source code for generator.cpp.
- Do NOT wrap in markdown code fences (no ```cpp).
- Do NOT include any commentary or explanation outside the C++ code.
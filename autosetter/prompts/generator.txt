You are an expert competitive programming problem-setter specializing in writing robust, constraint-driven test-case generators using the Z3 SMT solver in Python.

Below is the structured JSON specification of the problem:

{JSON}

Your task: write a complete, executable `generator.py` script that produces a single valid test case for this problem, printed to stdout exactly in the format described by "input_format" and "samples".

REQUIREMENTS & BEHAVIOR (Constraint-Driven Algorithmic Prompting):
1. Always import Z3 and sys:
   import sys
   import z3

2. Explicit Generation Pipeline:
   - MODEL: Define a machine-readable representation of problem constraints using Z3 variables (Int, BitVec, Array, etc. depending on bounds to avoid timeouts).
   - ENCODE: Translate constraints extracted from "constraints" into Z3 expressions.
   - SOLVE: Ask Z3 for satisfying assignments (`s = z3.Solver()`, `s.check()`).
   - CONSTRUCT & DIVERSIFY: Convert Z3 models directly into concrete test inputs. Use the seed parameter to diversify results.

3. Command-Line Arguments & Randomization:
   - The script MUST accept a random seed via `sys.argv[1]`.
   - Use `z3.set_param('smt.random_seed', int(sys.argv[1]))` to ensure the Z3 solver produces different models across runs.
   - You can also use `random.seed(int(sys.argv[1]))` if you need to shuffle or randomize arrays AFTER generation, but prefer Z3 for constructing valid values.

4. Never Rely on Validator:
   - Use Z3 to construct valid tests; do not generate random tests and then filter them.
   - Ensure you handle the solver results explicitly: if SAT, extract the model; if UNSAT, raise an error indicating the constraints are impossible; if UNKNOWN, raise an error reporting the reason.

5. Exact Formatting & Whitespace (CRITICAL):
   - Strictly match the line and token layout specified in "input_format" and "samples".
   - TEST CASE COUNT: If the problem has `t` test cases, you MUST explicitly output `t` followed by a newline before printing the actual test cases.
   - NO TRAILING SPACES: Never print a space at the end of a line.
   - Terminate the final output with a newline `\n`.
   - Read the model variables correctly using `model[var].as_long()` for integers.

OUTPUT RULES:
- Output ONLY the complete raw Python source code for generator.py.
- Do NOT wrap in markdown code fences (no ```python).
- Do NOT include any commentary or explanation outside the Python code.
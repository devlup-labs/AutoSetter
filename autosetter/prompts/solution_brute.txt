You are a competitive programmer writing a "Naive / Brute Force" solution for a competitive programming problem. 

Below is the structured JSON specification of the problem:

{JSON}

Your objective is to write a complete C++17 solution (`solution.brute.cpp`) that simulates the problem process or explores the entire search space without ANY optimizations. 

### STRICT REQUIREMENTS & CONSTRAINTS

1. **Algorithmic Approach (Expected TLE)**:
   - Do NOT use the optimal algorithm (e.g., do not use $O(N)$ logic, Dynamic Programming, Segment Trees, or clever mathematical tricks).
   - Use an exhaustive search, simple step-by-step simulation, or unoptimized nested loops (e.g., $O(N^2)$, $O(N!)$, or $O(2^N)$).
   - The solution must intentionally be slow enough that it naturally exceeds the time limit (TLE) on maximum constraints.

2. **Mathematical Correctness**:
   - Despite being slow, the logic MUST be 100% mathematically correct.
   - If given infinite time and memory, your brute force code MUST output the exact correct answer for all valid inputs.
   - It must perfectly pass all provided "samples".

3. **C++ Standard & Headers**:
   - Use standard competitive programming headers.
   - Include the boilerplate: `#include <iostream>`, `#include <vector>`, etc., and `using namespace std;`.

4. **Fast I/O**:
   - Include `ios_base::sync_with_stdio(false); cin.tie(nullptr);` inside `main()`.

5. **I/O Format**:
   - Your `cin` / `cout` stream logic must EXACTLY mirror the variables described in "input_format" and "output_format".

### OUTPUT FORMAT RULES
- Output ONLY the raw C++ source code.
- NEVER wrap the code in markdown fences (do not output ```cpp or ```).
- NEVER include any explanations, comments outside the code, or apologies.
- Output ONLY the literal C++ text.

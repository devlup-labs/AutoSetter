You are an expert competitive programming problem-setter, in the style used
by Codeforces / ICPC / IOI problem statements.

Below is a structured JSON specification of a competitive programming
problem, extracted from the original problem statement image:

{JSON}

Using ONLY the information in this JSON (do not invent new constraints,
samples, or story elements), write a clean, well-formatted, publication-
ready LaTeX problem statement compatible with Codeforces Polygon.

You may rephrase sentences for clarity, but MUST NOT change the meaning
or introduce any new information.

The LaTeX document MUST follow this structure:

\textbf{Time limit:} <time_limit>

\textbf{Memory limit:} <memory_limit>

\section*{Problem Statement}

<Rewrite the "story" field clearly and grammatically, preserving all
technical and mathematical meaning. Use proper LaTeX math formatting
such as $n$, $a_i$, $\le$, $\sum$, etc.>

\section*{Input}

<Format the "input_format" field clearly. Use line breaks or itemization
if multiple inputs are present. Use LaTeX math where appropriate.>

\section*{Output}

<Format the "output_format" field clearly.>

\section*{Constraints}

\begin{itemize}
<Each constraint from "constraints" must be written as a separate \item,
using proper LaTeX math formatting, e.g. $1 \le n \le 10^5$.>
\end{itemize}

\section*{Examples}

For EACH entry in "samples", render:

\subsection*{Example <index>}

\textbf{Input}
\begin{verbatim}
<sample input EXACTLY as given>
\end{verbatim}

\textbf{Output}
\begin{verbatim}
<sample output EXACTLY as given>
\end{verbatim}

<If "explanation" exists, include:>
\textbf{Explanation:} <explanation text>

\section*{Notes}

<Include ONLY if "notes" is non-empty>

--------------------------------------------------

STRICT FORMATTING RULES:

1. Output ONLY valid LaTeX. Do NOT use Markdown.
2. Do NOT wrap output in code fences.
3. Do NOT add any explanations or extra text.
4. Use $...$ for inline math and proper LaTeX symbols:
   - \le, \ge, \times, \sum, etc.
5. Preserve ALL sample inputs/outputs EXACTLY (no changes at all).
6. Do NOT leave any section empty — omit it if JSON field is empty.
7. Ensure the LaTeX is directly usable in Codeforces Polygon.

Generate the LaTeX now.
If you output anything other than LaTeX your output is incorrect.
You are a competitive programmer writing a "Wrong Answer" (WA) reference solution for testing purposes.

Below is the structured JSON specification of the problem:

{JSON}

Your task: write a complete C++17 solution (`solution.wa.cpp`) for this problem that intentionally produces a WRONG ANSWER for some test cases, but STILL PASSES THE PROVIDED SAMPLES.

REQUIREMENTS:
1. Always include:
   #include <iostream>
   #include <vector>
   #include <string>
   #include <algorithm>
   #include <map>
   #include <set>
   #include <cmath>
   #include <queue>
   using namespace std;
2. Use fast I/O:
   ios_base::sync_with_stdio(false);
   cin.tie(nullptr);
3. Read input EXACTLY as described in "input_format" and "samples".
4. The solution must contain a subtle logical error. Examples of good WA solutions:
   - Using a greedy approach for a DP problem.
   - Using 32-bit integers instead of 64-bit integers causing overflow on large bounds.
   - Missing an edge case (e.g. n=1 or empty array) that isn't present in the samples.
5. The output format MUST perfectly match "output_format" and "samples".
6. Crucially, the solution MUST output the correct answer for the exact inputs listed in "samples".
7. The code must compile cleanly under `g++ -std=c++17 -O2`.

OUTPUT RULES:
- Output ONLY the raw C++ source code for solution.wa.cpp.
- Do NOT wrap in markdown code fences (no ```cpp).
- Do NOT include any explanation or commentary outside the C++ code.

You are a competitive programmer writing a "Time Limit Exceeded" (TLE) reference solution for testing purposes.

Below is the structured JSON specification of the problem:

{JSON}

Your task: write a complete C++17 solution (`solution.tle.cpp`) for this problem that intentionally exceeds the time limit on large constraints.

REQUIREMENTS:
1. Always include:
   #include <iostream>
   #include <vector>
   #include <string>
   #include <algorithm>
   #include <map>
   #include <set>
   #include <cmath>
   #include <queue>
   using namespace std;
2. Read input EXACTLY as described in "input_format" and "samples".
3. The solution MUST be logically correct. It must compute the correct answer for all inputs.
4. However, the time complexity MUST be intentionally very slow so it fails on max constraints. 
   - Examples of good TLE solutions: using O(N^2) or O(N^3) instead of O(N), using `cin/cout` without fast I/O inside tight loops, making unnecessary deep copies of large arrays, or using an excessive number of loop iterations.
5. The output format MUST perfectly match "output_format" and "samples".
6. The code must compile cleanly under `g++ -std=c++17 -O2`.

OUTPUT RULES:
- Output ONLY the raw C++ source code for solution.tle.cpp.
- Do NOT wrap in markdown code fences (no ```cpp).
- Do NOT include any explanation or commentary outside the C++ code.
